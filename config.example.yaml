debug: true

# External services
services:
  avatars: 'https://a.localhost'
  cheesegull: 'https://storage.localhost'


# API server configuration
server:
  host: '0.0.0.0'
  port: 2100

  sentry_dns:

  database:
    driver: mysql
    dsn: 'root:toor@/osuserver'

  redis:
    host: '127.0.0.1:6379'
    password: ''
    db: 0

  mailgun:
    domain: ''
    private_key: ''
    public_key: ''
    from: ''